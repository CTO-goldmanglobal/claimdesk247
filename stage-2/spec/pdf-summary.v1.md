# PDF Customer Summary — Content Spec (Loop Request §8.1)
**Binding for Stage 2 (gate G-24).** This is the authoritative section list — do NOT best-guess. Generated server-side, sent via email/SMS link, deterministic content for testing.

## Required sections (in order)
1. **Reference number + date/time** — the session reference issued at S8.
2. **Accident details summary** — location (suburb/intersection), date/time, accident type, vehicles (user + other, fields as captured).
3. **Damage map (text description)** — damage locations both vehicles, rendered as text (no image in MVP — Phase 2).
4. **Evidence checklist** — items collected vs items outstanding (from S6 EVIDENCE).
5. **General information section** — the band output text for the matched scenario, **with the master disclaimer rendered inline** (resolve `{{ATTACH:master}}` → FIXED master string; G-18).
6. **Next steps** — what happens next, who calls, lawyer callback time preference captured at S7.
7. **Lawyer callback details** — booking summary + `{{CALLBACK_SLA}}` token.
8. **Full disclaimer block** — the FIXED `master` string in full.
9. **Footer (verbatim, FIXED):** `This summary was generated by an AI assistant and has not been reviewed by a lawyer. It is for your records only.`

## Hard rules
- No numeric fault % anywhere (G-17) — including the general-information section.
- No unresolved `{{...}}` tokens at render (G-18). Tokens resolve from the config map; `{{CALLBACK_SLA}}` etc. use stubbed dev values for Stage 2.
- No customer-facing case law.
- Plain English; any legal term explained or in a short glossary footer (CTP, without prejudice, etc. — reuse Loop Request Appendix B).
- This is the **customer** copy. The legal-firm intake brief (Loop Request §8.2) is Stage 3 — do not build it here.

## Test hook
`T-2-024` asserts master string present + zero unresolved tokens. Add a content-presence assertion that all 9 sections render for a completed `likely`-band rear-end intake.
